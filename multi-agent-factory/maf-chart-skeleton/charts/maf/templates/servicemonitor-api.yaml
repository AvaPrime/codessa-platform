apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: maf-api
  labels: { release: prometheus }
spec:
  selector: { matchLabels: { app: maf, component: api } }
  endpoints:
    - port: http
      interval: 15s
