apiVersion: v1
kind: Service
metadata:
  name: maf-api
spec:
  type: {{ .Values.service.type }}
  selector:
    app: maf-api
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 8000
      protocol: TCP
      name: http
